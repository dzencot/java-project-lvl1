plugins {
    id 'java'
    id 'application'
    id 'checkstyle'
}

compileJava {
    options.release = 17
}

group 'hexlet.code'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

application {
    mainClass = 'hexlet.code.App'
}

run {
    standardInput = System.in
}

test {
    useJUnitPlatform()
}